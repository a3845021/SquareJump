#ifndef __DEFINITIONS_H__
#define __DEFINITIONS_H__

const float GAME_SPEED = 0.45;

const float RAIN_SPEED = 0.35;
const float RAIN_DROPS_NUMBER = 100;
const float MIN_RAIN_DROP_SCALE = 0.5;
const float MAX_RAIN_DROP_SCALE = 2.5;
const GLubyte RAIN_DROP_OPACITY = 140; //(0 -> 255) unsigned char
const float RAIN_SCALE_SPEED_COEF = 0.6; // (0 -> 1) bigger it is then bigger
                                    // average speed of rain drops

const float FLIP_ANIMATION_TIME = 0.4; // seconds

#endif// __DEFINITIONS_H__
